package com.example.myapplication

import android.os.Bundle
import androidx.fragment.app.Fragment
import android.view.LayoutInflater
import android.view.View
import android.view.ViewGroup

class entryFragment : Fragment() {
    /*
    override fun onCreateView(
        inflater: LayoutInflater, container: ViewGroup?,
        savedInstanceState: Bundle?
    ): View? {
        return inflater.inflate(R.layout.fragment_main, container, false)
    }

    override fun onViewCreated(view: View, savedInstanceState: Bundle?) {
        super.onViewCreated(view, savedInstanceState)

        btn_fragcalculator1.setOnClickListener { onButtonClick(it) }
        btn_fragcalculator2.setOnClickListener { onButtonClick(it) }
        btn_fragcalendar.setOnClickListener { onButtonClick(it) }
        btn_fragroadmap1.setOnClickListener { onButtonClick(it) }
        btn_fragroadmap2.setOnClickListener { onButtonClick(it) }
    }

    fun onButtonClick(view: View) {
        // 버튼을 클릭했을 때 적절한 Fragment로 전환하는 내용 추가해야함....
    }
    */
}